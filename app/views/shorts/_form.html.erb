<%= form_for(@short) do |f| %>
  <% if @short.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@short.errors.count, "error") %> prohibited this short from being saved:</h2>

      <ul>
      <% @short.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Enter Long URL" %><br />
    <%= f.text_field :long %>
  </div>
<br/>
  <div class="field">
    <%= f.label "Description (It will come End of the short url)" %><br />
    <%= f.text_field :desc %>
  </div>
<br/>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
